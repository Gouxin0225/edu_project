<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.edubackend.mapper.StudentMistakeBookMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.edubackend.entity.StudentMistakeBook">
        <id column="id" property="id" />
        <result column="student_id" property="studentId" />
        <result column="question_id" property="questionId" />
        <result column="wrong_count" property="wrongCount" />
        <result column="last_wrong_time" property="lastWrongTime" />
        <result column="is_mastered" property="isMastered" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, student_id, question_id, wrong_count, last_wrong_time, is_mastered
    </sql>

</mapper>
